import React from 'react';

const NewsCard = ({news}) => {
    const {author, category_id, details, image_url, others_info, rating, thumbnail_url, title, _id} = news;
    console.log(news);
    return (
        <div>
            
        </div>
    );
};

export default NewsCard;